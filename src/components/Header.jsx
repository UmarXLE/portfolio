import React from 'react';
import styled from "@emotion/styled";

const Header = () => {
    return (
        <WrapperMain>

        </WrapperMain>
    );
};

const WrapperMain = styled('header')`
  
`

export default Header;